var numeros = prompt("Escribe un número y veamos si es par o impar:");

if (numeros % 2 === 0) {
  alert(" El numero " + (numeros) + " es Par")
} else {
  alert("El numero " + (numeros) + " es Impar")
}